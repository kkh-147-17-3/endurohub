<!-- This page permanently redirects to /community via +page.server.ts -->
<script lang="ts">
    // Unreachable — server-side 301 redirect fires before hydration.
    let { data } = $props();
</script>
